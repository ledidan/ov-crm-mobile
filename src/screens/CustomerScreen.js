import React from "react";
import CustomerScreenComponent from "../components/CustomerScreen";

export default function CustomerScreen({navigation}) {
  return <CustomerScreenComponent navigation={navigation}/>;
}
